<h1>予約フォーム</h1>

<ol class="nav">
	<li>1.入力</li>
	<li>2.確認</li>
	<li class="current">3.完了</li>
</ol>

<p>ご予約ありがとうございました。</p>

<h1>予約内容確定</h1>
<div>
   <h1>¥６０００/日</h1>
	<dl>
		
		<dt>開始日</dt>
		<dd><%= @reserve.start_date %></dd>
		<dt>終了日</dt>
		<dd><%= @reserve.end_date  %></dd>
		
		<dt>使用日数：</dt>
		<dd><%=  %></dd>
		<dt>人数：</dt>
		<dd><%= @reserve.num_people %></dd>
		<dt>合計金額</dt>
		<dd><%= %></dd>
	</dl>
	<p>上記内容でよろしければ「予約を確定」を押してください</p>

	<%= form_for @reserve, method: :post, url: reserve_complete_path do |form| %>
		<%= form.hidden_field :reserve_date %>
		<%= form.hidden_field :name %>
		<%= form.hidden_field :note %>
		<%= form.submit '予約を確定する' %>
	<% end %>

	<%= form_for @reserve, method: :post, url: reserve_back_path do |form| %>
		<%= form.hidden_field :reserve_date %>
		<%= form.hidden_field :name %>
		<%= form.hidden_field :note %>
		<%= form.submit 'ルーム詳細に戻る' %>
	<% end %>

	
</div>